# Backend origin the /api proxy forwards to (server-side). In dev this is the
# local backend; in prod set it to your deployed backend URL.
BACKEND_URL=http://localhost:4000

# Public origin of THIS site (used for sitemap.xml / robots.txt).
NEXT_PUBLIC_SITE_URL=http://localhost:3000
